<div class="page-title-box">
    <div class="row align-items-center">
        <div class="col-sm-6">
            <h4 class="page-title">{{title}}</h4>
        </div>
    </div>
    <div class="table-responsive">
        <table class="table">
            <tr>
                <td>Username</td>
                <td>:</td>
                <td>{{data.username}}</td>
            </tr>
            <tr>
                <td>Nama</td>
                <td>:</td>
                <td>{{data.fullname}}</td>
            </tr>
            <tr>
                <td>Role</td>
                <td>:</td>
                <td>{{data.role}}</td>
            </tr>
        </table>
        <a href="/" class="btn btn-secondary waves-effect waves-light">Kembali</a>
        <button type="button" class="btn btn-warning waves-effect waves-light" data-toggle="modal"
            data-target="#myModal">Ubah Sandi</button>
        <a href="/profile/edit" class="btn btn-info waves-effect waves-light">Ubah Profile</a>
    </div>
</div>

<div id="myModal" class="modal fade show" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-modal="true">
    <div class="modal-dialog">
        <form action="/profile/cpw" method="post">
            <input type="hidden" name="_csrf" value="{{csrf}}">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title mt-0" id="myModalLabel">Ubah sandi</h5><button type="button" class="close"
                        data-dismiss="modal" aria-hidden="true">×</button>
                </div>
                <div class="modal-body">
                    <div class="form-group">
                        <label for="">Sandi Baru</label>
                        <input type="password" name="password" id="password" class="form-control"
                            placeholder="Masukkan kata sandi baru" onkeyup='check();'>
                    </div>
                    <div class="form-group">
                        <label for="">Konfirmasi Sandi Baru</label>
                        <input type="password" name="passconf" id="passconf" class="form-control"
                            placeholder="Masukkan konfirmasi sandi baru" onkeyup='check();'>
                            <span id='message'></span>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary waves-effect" data-dismiss="modal">Close</button>
                    <button type="submit" id="btnSubmit" class="btn btn-success waves-effect waves-light">Change</button>
                </div>
            </div>
        </form>
    </div>
</div>

<script>
    document.getElementById('btnSubmit').disabled = true;
    var check = function() {
  if (document.getElementById('password').value ==
    document.getElementById('passconf').value) {
    document.getElementById('message').style.color = 'green';
    document.getElementById('message').innerHTML = 'Password dan konfirmasi password cocok';
    document.getElementById('btnSubmit').disabled = false;
  } else {
    document.getElementById('message').style.color = 'red';
    document.getElementById('message').innerHTML = 'Password dan konfirmasi password tidak cocok!';
    document.getElementById('btnSubmit').disabled = true;
  }
}
</script>